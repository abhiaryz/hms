'use client';

import { RegisterForm } from '@/components/auth/RegisterForm';

export default function RegisterPage() {
  return (
    <main className="h-screen flex items-center justify-center p-4 md:p-24 0">
      <RegisterForm />
    </main>
  );
} 